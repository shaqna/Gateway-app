<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BusApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/primaryColor</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:textColorHint">@color/grey</item>
        <item name="searchViewStyle">@style/SearchTheme</item>
    </style>

    <style name="SearchTheme" parent="Widget.AppCompat.Light.SearchView">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:background">@drawable/rounded_search_view</item>
        <item name="android:fontFamily">@font/inter</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="Theme.BusApp" parent="Base.Theme.BusApp" />

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">#000000</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">#000000</item>
        <!-- Used for the background -->
        <item name="android:background">#FFFFFF</item>
    </style>

    <style name="ButtonStyle.Poppins" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primaryColor</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_width">172dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="cornerRadius">8dp</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="ButtonStyle.Poppins.White" parent="Widget.Material3.Button.OutlinedButton">
        <item name="backgroundTint">@color/white</item>
        <item name="android:textColor">@color/primaryColor</item>
        <item name="strokeColor">@color/primaryColor</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:layout_width">172dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="cornerRadius">8dp</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>